# Cloudflare Pages — must match project name in dashboard
name = "kdmwaza-portfolio"
pages_build_output_dir = "dist"
compatibility_date = "2024-01-01"

# Dashboard deploy command (required when this file exists):
#   npx wrangler pages deploy dist --project-name=kdmwaza-portfolio
